# Copy to local.properties and adjust the values.
# Do not commit local.properties (it is in .gitignore).

sdk.dir=/Users/YOUR_USER/Library/Android/sdk

# Ktor backend on your Mac (physical device on the same Wi‑Fi).
# Mac IP: ipconfig getifaddr en0
api.base.url=http://192.168.1.42:8080/
