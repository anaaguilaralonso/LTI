# LTI monorepo — Docker Compose (esqueleto)
# Añade servicios cuando el backend esté definido.

services:
  # backend:
  #   build: ./backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - NODE_ENV=development
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: lti
  #     POSTGRES_PASSWORD: lti
  #     POSTGRES_DB: lti
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db_data:/var/lib/postgresql/data

# volumes:
#   db_data:
